function [x_point] = getpointgetinterpolation_focusYgetX(x_span, y_span, y_point, tolerance);
%y_span = y_span(:);
%y_span_test = y_span;
[~, ind_center] = min(abs(y_span - y_point));
ind_range = abs(y_span(1) - y_span(end))/tolerance; 
ind_low = ind_center - ind_range;
ind_high = ind_center + ind_range;
fprintf(ind_range);
plot(x_span())

end